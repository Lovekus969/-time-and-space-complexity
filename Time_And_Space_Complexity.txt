What is Time Complexity?

Time complexity tells us how the runtime of an algorithm grows as the size of the input (n) increases.
It’s a way to measure efficiency — how fast or slow your code runs independent of hardware.
